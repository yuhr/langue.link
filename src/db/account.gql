schema {
  query: Query
  mutation: Mutation
}

type Query {
  user(email: String!): User
}

type Mutation {
  user(email: String!): User
}

type User {
  uid: String!
  name: String!
  email: String!
  passwordHash: String!
  isTemporary: Boolean!
  dareRegistered: Int!
}